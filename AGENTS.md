# 注意事项

## 一、通用工程规约

- 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。
- 每次改动完成后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。
- 提交前确认 `.env` 未被跟踪 —— 里面有真实 API Key。
- **不做 UI 套壳**：后端工程（异常捕获、执行沙箱、成本控制）才是本项目的重点，
  前端只承担「把过程展示清楚」的职责。

## 二、架构决策（不可随意推翻）

### 2.1 不引入 LangGraph，手写 Function Calling 循环

Agent 循环（工具调用）**手写**，不引入 LangGraph 这类编排框架。

理由：本项目的循环只有「调模型 → 若返回 tool_calls 则执行 → 把结果回填 →
再调模型」这一个模式，状态线性，几十行 while 就能表达清楚。LangGraph 的
图 / 节点 / 状态机抽象是为「多分支 + 循环 + 人工介入 + 持久化」设计的，
用在这里属于杀鸡用牛刀，还会把「每一步模型看到了什么」藏进框架内部 ——
而这恰恰是我们需要能打印出来核对的东西。

**手写循环必须自带的三条硬约束**（已在 `rag-knowledge-base` 中验证有效）：

1. **步数上限**：`AGENT_MAX_STEPS`，用尽后摘掉工具再问一次强制收尾，
   避免无限循环烧 Token。
2. **工具错误回填**：执行失败时把结构化错误作为工具结果回填给模型，
   让它自己换招，而不是中断整个问答。
3. **配置错误不吞**：缺少 API Key 这类 `RuntimeError` 一律照常抛出，
   不被降级逻辑静默掩盖 —— 否则会变成「看起来在跑，其实一直没调通」。

### 2.2 不引入 LangChain

链路是「提取 schema → 拼 Prompt → 调模型 → 执行代码 → 回填 → 再调」，
每步都自己可控。框架在这一体量下只增加需要理解的黑盒抽象，收益为负。
需要多路检索融合或大量现成 Loader 时再按需引入具体组件。

### 2.3 执行沙箱用策略模式

- 默认实现：`DockerExecutor`（network=none、只读挂载、CPU/内存/PID 限额、
  超时强杀、OOM 与超时结构化区分）。
- 备选实现：`LocalSubprocessExecutor`（**仅本地调试**，明确生产禁用，
  启动日志带 UNSAFE 警告，非 dev 环境启动即失败）。
- **策略只读配置，绝不读模型输出** —— 让被执行的代码有机会改自己的执行
  策略，等于没有隔离。

详细设计见 `docs/sandbox-threat-model.md` 与 `docs/executor-interface.md`。

### 2.4 已确认的四条执行决策（2026-09-12）

1. **数据挂载走最小权限**：每次**只挂载本次会话实际用到的文件**
   （`ExecutionRequest.data_files`），按需复制到 `run_<id>/data/`。
   绝不挂载整个上传目录 —— 用户上传目录里可能有与本次提问无关的其他数据，
   挂进去就是无谓的暴露面。文件名一律 `safe_target_name()` 归一化，
   杜绝 `../../` 穿越把「副本」落到挂载目录之外。
2. **产物回传走文件系统**：代码写到 `/out`，宿主侧拷贝到持久的
   `artifact_dir`。**不采用 stdout base64** —— 那会撞上输出截断上限，
   图表这种几 MB 的产物会被切得七零八落。
3. **容器每次新建、用完即焚**：坚持 `docker run --rm`，接受百毫秒级启动
   开销，**不引入常驻容器**。换来的是环境绝对干净 —— 上一次运行不会以
   任何形式污染下一次（这也是我们不必处理「容器被跑挂后如何恢复」的原因）。
4. **保留最近 5 次运行目录**：`keep_recent_runs=5`。保留的是排查问题时
   唯一的现场（当时跑的代码 + 喂进去的数据 + 产出的图），由
   `_prune_old_runs()` 按 mtime 统一回收。

### 2.5 两个实现已知的语义差异

本地模式**没有 `/data` 挂载** —— 宿主机上不存在容器内的绝对路径。
所以本地模式下数据额外复制一份到工作目录，代码用**相对路径**访问。
这是本地执行器作为「调试工具」而非等价实现的又一佐证，编排层在 Schema
提取阶段应告知模型实际路径。

### 2.6 真机验证推翻的两条设计判据（2026-09-12）

### 2.7 手写 FC 循环的三条硬约束与一条坑（2026-09-12，`src/agent/`）

不引 LangGraph，手写 Function Calling 循环（§2.1 已立）。实现中踩到并固化的
三条硬约束加一条经验：

1. **步数上限**：`AGENT_MAX_STEPS` 用尽后必须**摘掉 tools** 再问一次强制收尾。
   这是循环唯一的兜底出口 —— 没有它，模型会在工具调用里无限打转。
2. **结果回填走 `to_tool_payload()`**：净化在执行层做完（去宿主路径、
   原始命令、artifacts 只给文件名），循环层直接透传，**字段名是
   `artifacts` 不是 `files`**（字段名不匹配让首次实现直接漏掉产物回传，
   测试 `test_artifacts_are_collected` 抓到了）。
3. **配置类错误照抛 `RuntimeError`**：缺 API Key 是 `ZhipuClient` 构造时就
   抛（**strip 后判空**，避免 `api_key="   "` 绕过）—— 不让它进循环，
   不让它消耗 token 才报错。
4. **hint 必须挂在回填消息的**末尾** —— 项目一踩过的坑：把约束放在
   system 提示里几乎无效，模型生成下一段代码时，最后读到的是什么就最影响
   它。同理：同错防撞墙时的升级提示也追在 hint 之后（仍是末尾）。

另两条踩出来的设计（都有回归测试）：

- **`run_python` 的入参只有 `code`，没有路径参数**。路径由编排层在系统
  提示里给死（"你当前可以访问的文件有：/data/sales.csv"）。给模型一个
  `data_path` 参数，它就会开始猜 —— 猜到宿主机其它路径就是隔离失效。
- **`arguments` 必须保持原始 JSON 字符串回填**。先 loads 再 dumps 会在
  模型输出不是严格 JSON 时破坏原样；项目一踩过，跨项目通用。

沙箱不是「写完就算对」的东西 —— 下面两条都是桩测试全绿、真机一跑才暴露的。
**凡是涉及容器行为的判据，必须有 `-m docker` 的真实集成测试兜底。**

1. **OOM 不要双条件**。`Killed` 是 shell 打印的，`docker run` 直接起 python
   没有 shell，OOM 时 stderr 为空（实测 `exit_code=137, stderr=""`）。
   要求「137 且含 Killed」会把 OOM 误判成代码错误，模型跑去改没写错的代码。
   → 判据：非超时的 137 即 OOM。
2. **判定词表不放裸 `oom`**。子串匹配会吃进 `boom`/`room`/`zoom`，
   把普通运行错误误报成 OOM。→ 只用 `out of memory`、`oom-kill` 等完整形式。

另外一条测试写法上的经验：隔离有**两道门**（静态预检 / 容器本身），
只写「正常写法」的用例永远只验到预检，容器那层没人守。隔离类用例一律
写成两段式 —— 先断言预检会拦，再用预检查不见的等价写法
（`importlib.import_module('soc' + 'ket')`、路径拼接）去撞容器。

### 2.8 前端的两条约束（2026-09-12，`web/index.html`）

1. **页面必须自包含，不引任何 CDN**。这个项目通篇在讲「执行环境要可控」，
   如果前端自己依赖一个外部脚本，故事就讲不圆了。语法高亮与 Markdown
   都是手写的极简实现，测试守住「没有 `<script src=>` 外链」。
2. **所有不可信文本走 `escapeHtml`**。模型输出的代码会被贴进页面，
   漏一处转义就是 XSS。纯字符串逻辑（高亮 / Markdown）由
   `tests/ui_render_check.js` 在 Node 里跑真实断言，不靠 Python 正则猜。

路径穿越也要在服务端再拦一道：`/api/artifact/{name}` 的文件名来自前端，
是不可信输入，`Session.artifact_path()` 会归一化 + 校验落点。

### 2.9 运行环境的「安全删除钩子」及其写法后果（2026-09-12）

本机（以及未来任何带类似垫片的环境）会在 `Path.unlink()` / `shutil.rmtree()`
上挂钩子：删除被重定向到回收站，且**批量删除计数超阈值时直接抛 `SystemExit`**。
`ignore_errors=True` 只忽略 `OSError`，拦不住它。踩到的三个后果：

1. 上传接口 500：替换数据时的 `unlink + move` 触发钩子 → 整个 ASGI 请求崩。
   **改法**：用 `os.replace` 原子改名（覆盖同名文件、完全不涉及删除）。
2. 执行失败：`_prune_old_runs` 的回收动作把一次**成功的执行**带崩。
   **改法**：清理一律 `try/except BaseException` 兜住，失败只警告一次。
3. 加 except 时写了 `logger.warning` 但该模块没定义 `logger` —— 把「被忽略的
   清理失败」升级成 `NameError`。桩测试抓不到（桩不会抛异常、进不了那个分支），
   所以新增了一条**主动让清理抛异常**的测试。

**规约**：清理/回收类动作一律视为附带行为，任何失败都不得影响主流程；
且必须有一条测试真的让清理失败一次。

### 2.8 前端的两条约束（2026-09-12，`web/index.html`）

1. **页面必须自包含，不引任何 CDN**。这个项目通篇在讲「执行环境要可控」，
   如果前端自己依赖一个外部脚本，故事就讲不圆了。语法高亮与 Markdown
   都是手写的极简实现，测试守住「没有 `<script src=>` 外链」。
2. **所有不可信文本走 `escapeHtml`**。模型输出的代码会被贴进页面，
   漏一处转义就是 XSS。纯字符串逻辑（高亮 / Markdown）由
   `tests/ui_render_check.js` 在 Node 里跑真实断言，不靠 Python 正则猜。

路径穿越也要在服务端再拦一道：`/api/artifact/{name}` 的文件名来自前端，
是不可信输入，`Session.artifact_path()` 会归一化 + 校验落点。

### 2.9 运行环境的「安全删除钩子」及其写法后果（2026-09-12）

本机（以及未来任何带类似垫片的环境）会在 `Path.unlink()` / `shutil.rmtree()`
上挂钩子：删除被重定向到回收站，且**批量删除计数超阈值时直接抛 `SystemExit`**。
`ignore_errors=True` 只忽略 `OSError`，拦不住它。踩到的三个后果：

1. 上传接口 500：替换数据时的 `unlink + move` 触发钩子 → 整个 ASGI 请求崩。
   **改法**：用 `os.replace` 原子改名（覆盖同名文件、完全不涉及删除）。
2. 执行失败：`_prune_old_runs` 的回收动作把一次**成功的执行**带崩。
   **改法**：清理一律 `try/except BaseException` 兜住，失败只警告一次。
3. 加 except 时写了 `logger.warning` 但该模块没定义 `logger` —— 把「被忽略的
   清理失败」升级成 `NameError`。桩测试抓不到（桩不会抛异常、进不了那个分支），
   所以新增了一条**主动让清理抛异常**的测试。

**规约**：清理/回收类动作一律视为附带行为，任何失败都不得影响主流程；
且必须有一条测试真的让清理失败一次。

## 三、成本控制（硬性规则）

调用 LLM 时，**绝不把完整数据集塞进 Prompt**。只给三样：

1. **Schema**：列名 + 类型 + 必要的语义说明；
2. **前 N 行样本**（默认 5 行）：让模型看懂数据长什么样；
3. **统计摘要**：行数、列数、缺失值、数值列的 min/max/均值等。

真实数据留在沙箱里，由生成的代码去读。一份 10 万行的 CSV 光塞原文就能烧掉
整个预算，而对「该写什么代码」几乎没有额外信息量 —— 模型需要的是**结构**，
不是**全量数据**。

配套要求：

- 每次模型调用的 token 用量要能统计和打印，便于发现回归。
- 多轮循环里历史消息按轮数 + 字符预算裁剪，不能无限增长。
- 大结果集不在工具返回里原样回填，只回填摘要 + 产物文件名。
- 沙箱输出有字节上限（默认 8KB），超限截断并显式标注 `*_truncated`，
  避免「模型基于残缺输出下结论」。

### 3.1 Schema 提取的实现要点（2026-09-12，`src/schema/extractor.py`）

成本控制的第一道闸门就在这里。四条规则，每条都对应一个真实翻车点：

1. **绝不 `read_csv(path)` 再 `.head()`**。任何一次读取都必须带 `nrows=` 或
   `usecols=`。这条纪律由 `TestNeverReadsFullTable` 用 spy 断言**调用参数**
   来守 —— 功能测试永远抓不到「改动让内存翻 100 倍」这类回归。
2. **算 mean/std/分位数必须读到整列**，与规则 1 有张力。解法是**按列裁剪**
   而非按行裁剪：数值列合并读一次（窄、省 IO），文本列逐列读
   （字符串列才是内存杀手）。内存占用始终只有一列。
   实测 8.9 MB / 5 万行的 CSV，完整提取耗时 **0.33 秒**。
3. **dtype 不能用 pandas 的粗粒度类型**。pandas 3.0 起字符串列是 `str` 而非
   `object`，但无论哪种都太宽泛 —— 必须拆成
   `string` / `category` / `mixed` / `datetime`，并给 `unique_count`，
   模型才知道能不能 groupby。
4. **给模型的路径必须是容器内路径**（`/data/xxx.csv`），绝不能是宿主的
   `D:\...`。映射用 `safe_target_name()` —— 与执行层挂载时**同一套规则**，
   保证模型拼出的路径一定打得开。

三个实现期踩到的坑（都已加回归测试）：

- **混合列检测不能靠 `isinstance`**：pandas 读 CSV 时整列统一成一个 dtype，
  数字 `0/1/2` 已被转成字符串，原始类型信息早就没了。只能反过来验 ——
  抽样看「有多少比例的值能解析回数字」，比例在 (0,1) 之间即 mixed。
- **样本太少时不能做比例推断**：5 行里 3 个取值，比例 0.6 远超 category 阈值
  0.5，会被误判成自由文本。行数 < 100 时只看 unique_count 绝对值。
- **`pd.NA` / `NaT` 必须显式转成 `None`**：它们既不是 float 也不是 None，
  漏掉会让整个 Schema 无法 JSON 序列化 —— 而这个对象是要直接进 API 请求体的。

## 四、本机工程环境的三条硬规约（踩坑沉淀）

这三条都是真实踩出来的，且**不止踩过一次**才固化。它们不是个人偏好，是团队级约定 ——
换个人、换一轮对话都容易重新踩进去，所以写在这里而不是留在某个人的笔记里。

### 4.1 不要用 PowerShell 内联传代码 —— 一律写成文件再执行

在 `pwsh -Command` 里内联传含引号 / 反引号的脚本会被**静默改坏**，而且改坏的方式
看起来和真实原因毫无关系：

- **双引号 here-string（`@"..."@`）把内容里的反引号当转义符**：`` `A `` 变成 BEL（`0x07`），
  `` | `JWT_SECRET` | `` 里的反引号直接消失 → 报错像是"文本变了"，而不是"引号有问题"。
- **单引号 here-string（`@'...'@`）吃掉内容里的双引号**：`pathlib.Path(r"D:\x")`
  变成 `pathlib.Path(rD:\x)` → 一行莫名其妙的 SyntaxError。
- **`.ps1` 文件本身必须是 UTF-8 with BOM**：PowerShell 5.1 对无 BOM 文件按 ANSI/GBK 解码，
  中文全变乱码，`-match '中文关键字'` 静默不命中 —— 表现为"功能坏了"，实际是读取方式错了。

**规约**：任何超过一行的 Python / JS / SQL 一律写成文件再执行（`python script.py`）。
需要 PowerShell 时也写成 `.ps1` 文件（**带 BOM**）再用 `-File` 调用，不内联。

### 4.2 批量文本替换：断言替换次数，全部成功才写盘

用脚本批量改文件（改文档、改配置、跨文件同步）时：

- **每处替换都断言「恰好命中 1 次」**。命中 0 次说明记错了原文 ——
  **立即停下来去看文件，不要猜、不要放宽匹配模式**；命中多次说明这串文本在别处也出现，
  盲目替换会误伤。
- **全部替换成功后才 `write_text`**。中途任何一处失败就整体不写盘。
  这一条真实救过一次：打印进度时在 GBK 控制台上遇到 `UnicodeEncodeError`（打不出 `⚠️`），
  异常发生在写盘之前，文件保持干净；否则会留下一个"改了一半"的文件，
  而且下次运行会因"原文已变"而全线 0 命中。
- 打印进度前设 `PYTHONIOENCODING=utf-8`，并避免直接输出 emoji / 生僻字符。

### 4.3 校验脚本自己要先能被信任

校验/冒烟脚本报 FAIL 时，**先怀疑脚本，再怀疑被测对象**：

- `Get-Content` 默认按 ANSI 解码，读 UTF-8 日志时中文变乱码，`-match '中文关键字'`
  静默失败 → 看起来像功能坏了。**读日志一律加 `-Encoding UTF8`。**
- 调 `curl.exe` 传 JSON 时不要当原生参数传：PowerShell 5.1 会打坏它，服务端收到畸形
  body 一律返 422，冒烟会"看起来全线崩"（实测曾误判 21 项失败）。写成临时文件走
  `--data-binary @file`。
- 一旦确认某条判据是**脚本自身造成的假失败**，就把原因写进脚本注释（"这不是功能坏了，
  是读取方式错了"），避免下一个人或下一轮重新排查一遍。

